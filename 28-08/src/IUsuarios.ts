//Crio a interface usuário
//Qualquer objeto ou classe que implemente/use esta interface PRECISA
//seguir as regras dela, ter todos os seus atributos, tipos e metodos
export interface IUsuario {
    nome: string;   
    idade: number;  
  }